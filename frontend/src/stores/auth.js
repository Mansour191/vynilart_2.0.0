/**
 * Auth Store - Re-export of the main auth store for compatibility
 * This file provides backward compatibility for imports from @/stores/auth
 */

// Re-export the main auth store
export { useAuthStore } from '../store/auth.js';
